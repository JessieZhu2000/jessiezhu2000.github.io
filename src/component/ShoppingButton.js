import React from 'react'

const ShoppingButton = ( { onClick }) => {
  return (
    <>
      <button className="button" onClick={onClick}>Shopping</button>
    </>
  )
}
export default ShoppingButton
